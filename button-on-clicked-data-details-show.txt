 
 <button class="btn btn-sm btn-outline--primary viewBtn" data-terms_of_trades="{{ $ad->terms }}" data-payment_details="{{ $ad->details }}" data-status='@php echo $ad->statusBadge @endphp' data-rate="{{ showAmount(getRate($ad)) }} {{ __($ad->fiat->code) }} / {{ __($ad->crypto->code) }}" data-maximum_limit="{{ showAmount($ad->max) }}" data-minimum_limit="{{ showAmount($ad->min) }}" data-margin='@php echo $ad->marginValue @endphp' data-payment_window="{{ $ad->window }} @lang('Minutes')" data-fiat_currency="{{ __($ad->fiat->name) }}" data-crypto_currency="{{ __($ad->crypto->name) }}" data-type='@php echo $ad->typeBadge @endphp' type="button">
                                                    <i class="las la-desktop"></i>@lang('Details')
                                                </button>
												
												
												
												
												 {{-- View Modal --}}
    <div class="modal fade" id="viewModal" role="dialog" tabindex="-1">
        <div class="modal-dialog modal-lg" role="document">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title"> @lang('Advertisement Details')</h5>
                    <button class="close" data-bs-dismiss="modal" type="button" aria-label="Close">
                        <i class="las la-times"></i>
                    </button>
                </div>

                <div class="modal-body">
                    <ul class="list-group list-group-flush advertisementDetails"></ul>
                </div>
            </div>
        </div>
    </div>






$('.viewBtn').on('click', function() {
                var modal = $('#viewModal');
                let data = $(this).data();
                let content = ``;
                $.each(data, function(index, value) {
                    content += `
                        <li class="list-group-item d-flex justify-content-between align-items-center flex-wrap">
                            <span >@lang('${titleCase(index)}')</span>
                            <span class="type">${value}</span>
                        </li>
                    `;
                });
                $('.advertisementDetails').html(content);
                modal.modal('show');
            });




//this method drop master.blade.php///////////
 function titleCase(string) {
                return string.replaceAll("_", ' ').replace(/\w\S*/g, function(txt) {
                    return txt.charAt(0).toUpperCase() + txt.substr(1).toLowerCase();
                });
            };